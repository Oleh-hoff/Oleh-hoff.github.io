# Amazon CRM

Дашборд аналитики Amazon на GitHub Pages. Меню слева, рабочая область справа.

Разделы: **Проверка аккаунта** (состояние по одиннадцати пунктам),
**Продажи по неделям** (три месяца в разрезе площадки и товара),
**Еженедельный анализ** (оценка недели по спецификации процесса),
**Реклама** (помесячные траты против прошлого года) и
**Вики** (база знаний проекта).

**Адрес:** https://oleh-hoff.github.io/

## Вход

Логин `oleh`, пароль задаётся в `assets/js/auth.js` (хранится не сам пароль,
а производная PBKDF2-SHA256, 150 000 итераций).

Сменить: открыть `tools/password.html`, ввести новый пароль, полученную
строку вставить в массив `users` файла `assets/js/auth.js`, закоммитить.

## Что здесь публично

Репозиторий публичный — иначе GitHub Pages на тарифе Free не работает.
Отсюда следует:

- **`data/finance.json` открыт по прямой ссылке.** Форма входа защищает
  страницу, но не файл: выручку, комиссии и маржу увидит любой, кто откроет
  адрес файла напрямую.
- Проверка логина выполняется в браузере посетителя, поэтому её можно
  обойти, открыв `app.html` напрямую.

Секреты SP-API при этом закрыты: они лежат в Actions secrets, их нет ни в
коде, ни в логах, и рабочим процессам из форков они недоступны.

Если публичность данных не устраивает — данные можно шифровать AES-256-GCM
ключом из пароля входа. Тогда по прямой ссылке будет нечитаемый набор байт,
а всё остальное останется как есть.

## Автоматизация

`.github/workflows/amazon-sync.yml` — каждые 4 часа забирает новые проводки
из SP-API, обновляет `data/finance.json` и коммитит. Pages пересобирает сайт
сам, поэтому цифры на дашборде обновляются без участия человека.

Режимы запуска вручную (Actions → Run workflow):

| Режим | Что делает |
|---|---|
| `incremental` | добрать новое с последней отметки — минуты |
| `resume` | продолжить сбор истории с места обрыва, пять окон за заход |
| `full` | заново с начала года — часы |

История собирается заходами `resume`: лимит Finances API — 0,5 запроса в
секунду, за год набирается около четырёх тысяч страниц. Один заход берёт
пять окон по 15 дней и коммитит результат, поэтому прерывание стоит не
больше одного окна.

## Технический стек

**Собирать нечего — коммит становится сайтом.** Шага сборки нет, и потому нет
ни расхождения «локально собралось, в CI нет», ни устаревшего артефакта.

| Слой | Чем | Чего намеренно нет |
|---|---|---|
| Страницы | статический HTML, ES-модули, CSS | бандлера, фреймворка |
| Стили | CSS custom properties, `data-theme` | препроцессора, Tailwind |
| Графики | свой SVG-модуль `charts.js` | Chart.js, D3 |
| Markdown вики | свой разбор `markdown.js` | marked, markdown-it |
| Языки | свой словарь, ru / en / uk | i18next |
| Вход | PBKDF2-SHA256, 150 000 итераций, WebCrypto | сервера, сессий, БД |
| Данные | статические JSON в `data/`, читаются `fetch` | API, бэкенда |
| Сборщики | Python 3.12, только стандартная библиотека | requests, boto3, pandas |
| Клиент SP-API | свой, LWA-токен в заголовке | AWS SigV4, SDK Amazon |
| Хостинг | GitHub Pages из `main` | Cloudflare, Vercel |
| Автоматизация | GitHub Actions, крон каждые 4 часа | внешнего планировщика |
| Проверки | jsdom, ставится локально | зависимостей в репозитории |

**Сторонних запросов со страницы — ноль.** Ни CDN, ни шрифтов, ни аналитики:
всё, что грузит открытая страница, лежит в этом же репозитории. Отсюда и свои
графики, свой markdown, свой словарь.

Внешние источники есть только у сборщиков: Amazon SP-API и справочные курсы
Европейского центробанка. Браузер читает уже готовые JSON.

Из возможностей браузера нужны ES-модули, `fetch`, `localStorage` и
`sessionStorage`, WebCrypto, `Intl`, `ResizeObserver`, `matchMedia`,
`<dialog>` и SVG. Полифилов нет.

Подробнее — статья «Технический стек» в разделе Вики.

## Структура

```
index.html              страница входа
app.html                CRM: меню + рабочая область
assets/css/             токены, база, оболочка, графики
assets/js/              i18n, темы, вход, форматы, графики, данные
assets/js/views/        разделы CRM
assets/js/weekly-analysis-engine.js  расчёты еженедельного анализа, без DOM
assets/js/fba-spec.js   поля пульта дефицита и их источники в SP-API
assets/js/markdown.js   разбор markdown для вики, без зависимостей
assets/js/settings.js   окно настроек: язык, часовой пояс, тема
assets/js/timezone.js   выбранный часовой пояс
data/finance.json       проводки: дата × площадка × валюта × статья
data/weekly-sales.json  продажи: неделя × площадка × ASIN
data/wiki/              статьи вики: index.json и <имя>.<язык>.md
data/fx-rates.json      справочные курсы ЕЦБ для перевода в евро
data/promotions.json    купоны и дилы для отметок над неделями
scripts/collect_amazon.py         сборщик проводок
scripts/collect_weekly_sales.py   сборщик недельных продаж
scripts/collect_fx_rates.py       курсы ЕЦБ
scripts/collect_promotions.py    купоны и дилы
spapi/                  клиент SP-API
tools/password.html     генератор хэша пароля
tools/mount-check.mjs   монтаж раздела в headless-DOM
tools/shell-check.mjs   настройки и журнал синхронизаций
tools/numbers-check.mjs сверка чисел раздела продаж
tools/analysis-check.mjs сверка чисел еженедельного анализа
```

## Данные

**Проводки** — Finances API. Выручка и комиссии приходят из одних и тех же
событий, поэтому сходятся между собой.

**Продажи по товарам** — отчёт по заказам
(`GET_FLAT_FILE_ALL_ORDERS_DATA_BY_ORDER_DATE_GENERAL`). Отчёт отдаёт максимум
30 дней за запрос, поэтому квартал берётся четырьмя отчётами. Родитель товара
у Amazon свой на каждой площадке, поэтому вариации сшиваются в семьи.

Статьи расходов: Referral Fee, логистика FBA, размещение запасов, хранение,
поставка на склад и пошлины, реклама, купоны и акции, прочие сборы,
возвраты, возмещения Amazon. Незнакомый тип не теряется — попадает в
«прочее» под своим именем и печатается списком в конце запуска.

Суммы по умолчанию не пересчитываются между валютами: курса в данных Amazon
нет, а взятый со стороны не сойдётся ни с Seller Central, ни с банковской
выпиской. Валюта выбирается фильтром, а «вместе по всем маркетплейсам»
считается в штуках — они складываются без всякого курса.

Для вопроса «сколько всего» в разделе продаж есть режим **«Всё в евро»** по
справочным курсам ЕЦБ (`scripts/collect_fx_rates.py`), понедельно. Он подписан
как оценка и с Seller Central не сойдётся — это ориентир масштаба.

**Акции.** Над столбцами недель стоят отметки купонов и дилов: круг — купон,
ромб — бест-дил, треугольник — молниеносный. Отметка ставится при пересечении
кампании с неделей хотя бы одним днём и уважает фильтр по товару. Отменённые
дилы не отмечаются: акции не было.

**Еженедельный анализ** — раздел по отдельной спецификации процесса
(`Sales/schotyzhnevyi-analiz.html` в приватном репозитории). Четыре вкладки:
сводный дашборд и три анализа. Описан целиком только анализ продаж; BSR и
органика ждут подключения API сторонних инструментов и остаются заглушками.

Считается то, что задано: уровень как среднее трёх чистых недель перед
базовой (откат не дальше двух месяцев), Фактор 1 — отклонение базовой недели
от уровня по семи полосам, экстремумы за три месяца, глубина истории.
Там, где спецификация правила не задала, раздел ставит помету со ссылкой на
номер открытого вопроса, а не подставляет число: Фактор 2 (правило
направления), итог по двум факторам, сток и порог наличия, оценка недели для
парент-вариации.

Прапорцы событий собираются только по купонам и дилам — три из двадцати из
списка спецификации. Остальные семнадцать (out of stock, смена цены, Prime
Day, действия конкурентов) не фиксируются нигде, и раздел говорит об этом
прямо: «чистая неделя» пока означает только «без купонов и дилов».

**Настройки** — кнопка в правом верхнем углу на всех страницах: язык
(українська / русский / English), часовой пояс и тема. Пояс применяется к
отметкам времени, но не к календарным датам.

## Локальный запуск

```bash
python3 -m http.server 8000
```

ES-модули и WebCrypto требуют http — с двойного клика по файлу не заработает.

Проверка раздела без браузера:

```bash
npm i jsdom
node tools/mount-check.mjs weekly-sales
node tools/mount-check.mjs wiki
node tools/shell-check.mjs
node tools/numbers-check.mjs
node tools/mount-check.mjs weekly-analysis
node tools/analysis-check.mjs
```
