# --- Секреты: никогда не коммитить -----------------------------------------
.env
*.env
*.pem
*.key
credentials.json

# --- OS / IDE ---------------------------------------------------------------
.DS_Store
.idea/
.vscode/
*.swp

# --- Python -----------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
# --- Node ------------------------------------------------------------------
# jsdom ставится локально ради tools/*-check.mjs и в репозитории не нужен:
# Pages отдаёт файлы как есть, а Actions собирают данные, а не страницу
node_modules/
package-lock.json
package.json
